<%= image_tag @content.image %>

<h1><%= @content.title %></h1>

<%= simple_format @content.body %>

<!-- Use this variable to loop over each review and pull the content to the homepage -->
<!-- Show review score and user name. Or annoymous if there was no name -->
<h2>Highly rated reviews</h2>
  <%= render partial: "review", collection: @highly_rated_reviews %>

<h2>Bien featured reviews</h2>
<%= render partial: "review", collection: @featured_reviews %>
